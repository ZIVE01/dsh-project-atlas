# Third-party notices

The package has no runtime or development package dependencies. It uses only
standard modules supplied by the Node.js runtime. The runtime is not bundled or
redistributed with this source package and remains governed by its own license.
